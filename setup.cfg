# Style configuration for flake8. Declared here so that a bare `flake8` run
# from the repository root uses the same line length the code was written to,
# rather than the 79-character default.
[flake8]
max-line-length = 88
exclude = .venv,.git,__pycache__,.ipynb_checkpoints,data
